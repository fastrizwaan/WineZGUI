       # Test if exe exists?
       Locate_Exe_Function

       export base=$(basename "${EXE_FILE}")
       export dire=$(dirname  "${EXE_FILE}")

       cd "$dire"
       echo "I: Script: Launching ${base}"
       ${WINE_CMD} "${base}"

       # Not exiting for test purpose
       # exit 0; # Do Exit after execution of exe
